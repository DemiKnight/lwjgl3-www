import type { ScriptState } from '../BuildScript';

export function generateSbt({
                              build,
                              version,
                              hardcoded,
                              compact,
                              osgi,
                              platform,
                              platformSingle,
                              artifacts,
                              selected,
                              addons,
                              selectedAddons,
                            }: ScriptState) {
  let script = '';



  return script;
}
